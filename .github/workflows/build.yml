name: Build and Release Ralph

on:
  push:
    paths:
      - '**.py'
      - 'requirements.txt'
      - 'setup.py'
      - '.github/workflows/build.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Game
    runs-on: ubuntu-latest


    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install setuptools

      - name: Build application
        run: |
          python setup.py build_apps

      - name: Archive Windows build
        run: |
          cd build/win_amd64
          zip -r ../../Ralph-Windows.zip .

      - name: Archive Linux build
        run: |
          cd build/manylinux2014_x86_64
          zip -r ../../Ralph-Linux.zip .

      - name: Archive macOS build
        run: |
          cd build/macosx_10_13_x86_64
          zip -r ../../Ralph-macOS.zip .

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: Ralph Release v1.0.${{ github.run_number }}
          files: |
            Ralph-Windows.zip
            Ralph-Linux.zip
            Ralph-macOS.zip

      - name: Setup Butler
        uses: remarkablegames/setup-butler@v1

      - name: Publish Windows to itch.io via Butler
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}
        run: |
          mkdir -p push_win
          cp Ralph-Windows.zip push_win/
          butler push push_win shivamkr12/roaming-ralph:windows --userversion v1.0.${{ github.run_number }}

      - name: Publish Linux to itch.io via Butler
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}
        run: |
          mkdir -p push_linux
          cp Ralph-Linux.zip push_linux/
          butler push push_linux shivamkr12/roaming-ralph:linux --userversion v1.0.${{ github.run_number }}

      - name: Publish macOS to itch.io via Butler
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}
        run: |
          mkdir -p push_mac
          cp Ralph-macOS.zip push_mac/
          butler push push_mac shivamkr12/roaming-ralph:mac --userversion v1.0.${{ github.run_number }}
